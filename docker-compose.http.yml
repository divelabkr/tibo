# HTTP only — 공유기/방화벽에서 80 포트를 열거나 LAN 접속용
# docker compose -f docker-compose.yml -f docker-compose.http.yml up -d
services:
  tibo:
    ports:
      - "${HTTP_PORT:-80}:80"
    labels:
      - com.centurylinklabs.watchtower.enable=true
