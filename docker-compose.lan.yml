# Windows: 80/443/8080 이 예약·점유된 경우가 많음 → 18080
# docker compose -f docker-compose.yml -f docker-compose.lan.yml up -d
# 브라우저: http://127.0.0.1:18080
services:
  tibo:
    ports:
      - "18080:80"
    labels:
      - com.centurylinklabs.watchtower.enable=true
