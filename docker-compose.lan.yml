# Windows / 포트 충돌 시 로컬 확인용 (https 없이)
# docker compose -f docker-compose.yml -f docker-compose.lan.yml up -d
services:
  tibo:
    ports:
      - "${HTTP_PORT:-8080}:80"
    labels:
      - com.centurylinklabs.watchtower.enable=true
